[tool.poetry]
name = "loading"
version = "1.0.0"
description = "Zion system program loading module"
authors = ["eabushak"]

[tool.poetry.dependencies]
python = ">=3.10"
numpy = "^1.26.0"
pandas = "^2.2.0"
matplotlib = "^3.8.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
